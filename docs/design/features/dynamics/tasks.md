# The dynamics family, tasks

| Task | Requirement | State |
|---|---|---|
| Roles `state` and `independent`; `Query` and `Problem.queries`; schema 1.1 reading 1.0 | R-101, R-110 | done |
| The `rate` relation with its dimensional check | R-102 | done |
| The dynamics and optimization family checks; determinacy for states and the independent variable | R-103 to R-106 | done |
| Canonical form for rates and queries | R-107 | done |
| `planteo.evaluate` over the closed node set | R-108 | done |
| `planteo.emit.scipy` and its agreement with the evaluator | R-109 | done |

## Convergence

Recorded in `docs/design/SDD.md` section 10 with the release that ships the family.
